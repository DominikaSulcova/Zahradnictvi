// @ts-check
import { defineConfig } from 'astro/config';

// `site` will be set to https://zahradnictvi-usilne.cz once the domain is
// switched over — it's needed for the sitemap and SEO metadata (Milestone 5).
export default defineConfig({});
